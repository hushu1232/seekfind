{
  "name": "qiuwen-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/build-content.js && node scripts/build.js chrome",
    "build:chrome": "tsc && vite build && node scripts/build-content.js && node scripts/build.js chrome",
    "build:firefox": "tsc && vite build && node scripts/build-content.js && node scripts/build.js firefox",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.173.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^25.9.1",
    "@types/three": "^0.173.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}

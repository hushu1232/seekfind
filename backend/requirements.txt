# ============================================================
#  求问 — 后端依赖锁定
# ============================================================

# --- Web 框架 ---
fastapi==0.115.*
uvicorn[standard]==0.34.*
websockets==14.*

# --- Agent 引擎 ---
langgraph==0.3.*
langchain-core==0.3.*
langchain-openai==0.3.*

# --- 向量库 ---
chromadb==0.6.*

# --- 检索 ---
rank-bm25==0.2.*
jieba==0.42.*

# --- Reranker（T2.1 检索质量提升）---
sentence-transformers>=2.2.0

# --- HTTP ---
httpx==0.28.*

# --- 文档清洗 ---
trafilatura==1.12.*

# --- Scrapling（自适应爬取 + 反检测）---
# 核心包（HTTP 模式，基于 curl_cffi，无需 playwright）
scrapling>=0.4.8

# --- 浏览器指纹生成（可选）---
browserforge>=0.3.0

# --- 配置 ---
pydantic-settings==2.*

# --- 日志 ---
structlog==24.*

# --- 监控 (P1) ---
prometheus_client>=0.19.0

# --- 图像处理 ---
Pillow==11.*

# --- Ollama 客户端 ---
ollama==0.4.*

# --- 文件上传 ---
python-multipart==0.0.*

# --- MCP Server ---
mcp>=1.0.0

# --- 语音合成 (Phase 4) ---
edge-tts==6.*

# --- 语音识别 (Phase 4, 可选) ---
# vosk==0.3.*

# --- 测试 ---
pytest==8.*
pytest-asyncio==0.24.*
